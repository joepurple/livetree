name: livetree

init:
  script: npm install

dev:
  dashboard:
    cmd: npm run dashboard
    portArg: -- --port

links:
  dashboard: ${url:dashboard}
  mobile: livetree://connect?url=${enc:tunnelUrl:dashboard}
