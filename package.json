{
  "name": "treeswitch",
  "version": "0.1.0",
  "description": "Switch a stable .live-tree/src symlink between Git worktrees.",
  "type": "module",
  "bin": {
    "treeswitch": "./dist/cli.js",
    "tsw": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
